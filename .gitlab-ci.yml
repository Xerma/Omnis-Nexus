image: node:20

stages:
  - deploy

deploy_railway:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "production"'
  before_script:
    - npm install -g @railway/cli
    - railway --version
  script:
    - railway up --ci